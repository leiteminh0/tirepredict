# OBRIGATÓRIO — sem esta variável o frontend não inicializa.
# Em desenvolvimento local: http://localhost:8000
# Em produção: URL do Railway ou outro host da API.
# NÃO use a URL de produção em desenvolvimento local — isso polui dados reais.
VITE_API_URL=http://localhost:8000
